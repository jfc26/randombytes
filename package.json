{
  "name": "zitska-randombytes",
  "version": "1.0.0",
  "description": "randombytes",
  "main": "build/index.js",
  "files": [
    "./build/",
    "./bin"
  ],

  "scripts": {
    "start": "node build/index.js",
    "dev": "nodemon src/index.ts",
    "prepare": "npm run build",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "randombytes": "./bin/index.js"
  },
  "keywords": ["randombytes"],
  "author": "MIT",
  "license": "ISC",
  "dependencies": {
    "commander": "^10.0.1",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@types/node": "^20.1.0"
  }
}
